Server:
  port: 8093
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: feign-client啊啊啊啊啊啊啊啊
feign:
  hystrix:
    enabled: trueffffffffffff
management:
  endpoints:
    web:
    exposure:
      include: health,info,env,metrics,beans

